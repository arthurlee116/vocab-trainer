# 修复计划：清理未提交变更中的minor问题

## 概述
基于审查，仅4个低/中优先级问题（console.error残留、注释痕迹、CSS体积）。无高优先级阻塞。计划使用SearchReplace精确修复，不改动逻辑。

## 步骤
1. **修复QuizPage.tsx console.error** (2处，中优先级)
   - L252 & L404：删除console.error，保留UI反馈。
2. **清理tts.ts注释** (低优先级)
   - L79：删除`// console.error`行。
3. **验证**：
   - 运行`vitest`确认测试通过。
   - `tsc --noEmit` 类型检查。
4. **无CSS变更**（仅建议，非立即）。

## 预期结果
- 移除调试日志，代码更生产就绪。
- 变更最小（<10行），测试不受影响。
- 完成后git status干净，可commit。

确认后执行。